# Stubs for Krylov extension
function cg!(args...)
    error("`import Krylov` to use function `complete`.")
end

function cgworkspace(args...)
    error("`import Krylov` to use function `complete`.")
end

function cgsolution(args...)
    error("`import Krylov` to use function `complete`.")
end
